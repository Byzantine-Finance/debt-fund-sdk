// Import ABIs using require to avoid TypeScript import issues
const VAULT_FACTORY_ABI = require("./VaultFactory.json");
const VAULT_ABI = require("./Vault.json");

export { VAULT_FACTORY_ABI, VAULT_ABI };
